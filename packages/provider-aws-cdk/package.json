{
    "name": "@up-sdk/provider-aws-cdk",
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
        "build": "yarn run clean && yarn run compile",
        "clean": "rm -rf ./dist",
        "compile": "tsc -p tsconfig.build.json",
        "prepublishOnly": "yarn run build",
        "test": "jest"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/aws-lambda": "^8.10.72",
        "@types/jest": "^26.0.20",
        "aws-lambda": "^1.0.6",
        "jest": "^26.6.3",
        "ts-jest": "^26.5.1",
        "typescript": "^4.1.5"
    }
}
